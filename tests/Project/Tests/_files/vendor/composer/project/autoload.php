<?php

// autoload.php @generated by Sinergi Project

use Sinergi\Project\Autoloader;

require_once dirname(dirname(__DIR__)) . "/sinergi/project/src/Project/Autoloader.php";

$autoloader = new Autoloader;
$autoloaders = require __DIR__ . DIRECTORY_SEPARATOR . 'autoload_psr4.php';

if (isset($autoloaders)) {
    foreach ($autoloaders as $namespace => $paths) {
        foreach ($paths as $path) {
            $autoloader->addNamespace($namespace, $path);
        }
    }
}
$autoloader->register();
